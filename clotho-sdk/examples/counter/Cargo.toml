[package]
name = "counter"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "counter"
path = "main.rs"

[dependencies]
clotho-sdk = { path = "../.." }
tokio = { version = "1.0", features = ["sync", "macros", "io-util", "rt", "time"] }
anyhow = "1.0"
async-trait = "0.1"
