[package]
name = "daily-idea"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "daily-idea"
path = "main.rs"

[dependencies]
clotho-sdk = { path = "../.." }
tokio = { version = "1.0", features = ["sync", "macros", "io-util", "rt", "time"] }
anyhow = "1.0"
